import React from 'react';

interface ProfileSelectionDialogProps {
}

const ProfileSelectionDialog: React.FC<ProfileSelectionDialogProps> = (props) => {
  return (
    <p>ProfileSelectionDialog</p>
  );
};

export default ProfileSelectionDialog;
