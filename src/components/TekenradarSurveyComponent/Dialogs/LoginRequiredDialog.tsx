import React from 'react';

interface LoginRequiredDialogProps {
}

const LoginRequiredDialog: React.FC<LoginRequiredDialogProps> = (props) => {
  return (
    <p>LoginRequiredDialog</p>
  );
};

export default LoginRequiredDialog;
